$(".alerts").append("<%= (j render(
    :partial => 'layouts/alerts', :locals => { :alert => @alert, :notice => @notice }
)) %>");

<% if @notice %>
  $("#league_" + + <%= @ul.league.id %>).find(".actions").html('<%= render html: render_leave(@ul).html_safe %>')
<% end %>
