<div class='row'>
  <div class='col-lg-8 col-lg-offset-2'>
    <div class="box box-primary">
      <div class="box-header with-border">
        <h3 class="box-title">Edycja zawodnika: <%= @player.name %></h3>
      </div>
      <%= form_for @player do |f| %>
        <div class="box-body">
          <div class="form-group">
            <label>Gole</label>
            <%= f.number_field :goals, class: 'form-control'%>
          </div>
          <div class="form-group">
            <label>Asysty</label>
            <%= f.number_field :assists, class: 'form-control'%>
          </div>
          <div class="box-footer">
            <%= f.submit "Aktualizuj", class: 'btn btn-primary' %>
            <%= link_to "Powrót", players_path, :class => "btn btn-warning pull-right" %>
        </div>
        </div>
        
      <% end %>
    </div>
  </div>
</div>