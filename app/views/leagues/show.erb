<div class='row'>
  <div class='col-lg-6 col-lg-offset-3'>
    <div class="box box-primary text-center">
      <div class="box-header">
        <h3 class="box-title"><%= @league.name %></h3>
      </div>
      <div class="box-body no-padding">
        <table class="table table-striped table-bordered">
          <tbody>
            <tr>
              <th>Miejsce</th>
              <th>Imię i nazwisko</th>
              <th>Punkty</th>
              <th>Trafienia</th>
            </tr>
            <% i = 1 %>
            <% @users.each do |user| %>
              <tr id="user_<%= user[:id] %>">
                <td class="js-place"> <%= user["place"] %> </td>
                <td class="js-name"><%= user["first_name"] %> <%= user["last_name"] %></td>
                <td class="js-points"><%= user["points"] %></td>
                <td class="js-exact"><%= user["bets_count"] %></td>
              </tr>
            <% end %>
          </body>
        </table>
      </div>
    </div>
  </div>
</div>