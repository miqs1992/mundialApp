<div class='row'>
  <div class='col-lg-12'>
    <div class="box box-primary text-center">
      <div class="box-header">
        <h3 class="box-title"><%= @league.name %></h3>
      </div>
      <div class="box-body no-padding">
        <table class="table table-striped table-bordered">
          <tbody>
            <tr>
              <th>Miejsce</th>
              <th>Imię i nazwisko</th>
              <th>Punkty</th>
            </tr>
            <% i = 1 %>
            <% @league.users.each do |user| %>
              <tr id="user_<%= user.id %>">
                <td class="js-place"> <%= @league.place(user) %> </td>
                <td class="js-name"><%= user.name %></td>
                <td class="js-points"><%= user.points %></td>
              </tr>
            <% end %>
          </body>
        </table>
      </div>
    </div>
  </div>
</div>