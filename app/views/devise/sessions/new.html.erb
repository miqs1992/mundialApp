<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Mundial Typer | Log in</title>
  <meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>
  <%= javascript_pack_tag 'application' %>
  <%= stylesheet_pack_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body class="login-page">
<div class="login-box">
  <div class="login-logo">
    <a href="/">Mundial Typer</a>
  </div>
  <%= render 'layouts/alerts' %>
  <div class="login-box-body">
    <p class="login-box-msg">Zaloguj się do aplikacji</p>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group has-feedback">
          <%= f.text_field :devise_login, class: 'form-control', autofocus: true, placeholder: "Login lub E-mail" %>
          <span class="glyphicon glyphicon-user form-control-feedback"></span>
        </div>

        <div class="form-group has-feedback">
          <%= f.password_field :password, class: 'form-control', autocomplete: 'off', placeholder: "Hasło" %>
          <span class="glyphicon glyphicon-lock form-control-feedback"></span>
        </div>

        <div class="row">
          <div class="col-xs-offset-8 col-xs-4">
            <%= f.submit 'Zaloguj', class: 'btn btn-primary btn-block btn-flat' %>
          </div>
        </div>
    <% end %>
  </div>
</div>
</body>
</html>
