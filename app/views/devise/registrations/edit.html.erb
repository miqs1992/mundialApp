<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Mundial Typer</title>
  <meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>
  <%= javascript_pack_tag 'application' %>
  <%= stylesheet_pack_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body class="login-page">
<div class="login-box">
  <div class="login-logo">
    <a href="/">Mundial Typer</a>
  </div>
  <%= render 'devise/shared/alerts' %>
  <div class="login-box-body">
    <p class="login-box-msg">Aktualizacja danych</p>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :patch }) do |f| %>

        <div class="form-group has-feedback">
          <%= f.text_field :email, class: 'form-control', autofocus: true, autocomplete: "email", placeholder: "E-mail"  %>
          <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
        </div>

        <div class="form-group has-feedback">
          <%= f.text_field :login, class: 'form-control', autofocus: true, autocomplete: "login", placeholder: "Login"  %>
          <span class="glyphicon glyphicon-log-in form-control-feedback"></span>
        </div>

        <div class="form-group has-feedback">
          <%= f.text_field :first_name, class: 'form-control', autofocus: true, autocomplete: "first_name", placeholder: "Imię"  %>
          <span class="glyphicon glyphicon-user form-control-feedback"></span>
        </div>

        <div class="form-group has-feedback">
          <%= f.text_field :last_name, class: 'form-control', autofocus: true, autocomplete: "last_name", placeholder: "Nazwisko"  %>
          <span class="glyphicon glyphicon-user form-control-feedback"></span>
        </div>

        <div class="form-group has-feedback">
          <%= f.password_field :current_password, class: 'form-control', autofocus: true, autocomplete: "off", placeholder: "Obecne hasło - wymagane"  %>
          <span class="glyphicon glyphicon-lock form-control-feedback"></span>
        </div>

        <div class="actions ">
          <%= f.submit "Zaktualizuj", class: 'btn btn-primary btn-block btn-flat' %>
        </div>
    <% end %>
    <div>
      <%= link_to "Powrót", root_path %>
    </div>
  </div>
</div>
</body>
</html>